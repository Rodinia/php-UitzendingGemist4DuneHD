<?php

    // Set to true to enable MySQL backend
    $useMySQL       = false;
    
    // MySQL config (ignored if $useMySQL == false)
    $mysql_host     = "localhost";
    $mysql_database = "ug";
    $mysql_user     = "root";
    $mysql_password = "";

    // If set to true it will use CURL as much as possible to get remote content
    $useCurlLoad = true;
?>